server {
    listen 80;
    server_name _;

    # Serve admin SPA from /usr/share/nginx/html
    # Traefik strips /admin prefix before forwarding here
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
